using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
	    //Debug.Log("###############" + this.transform.localPosition);


	    //var pivotOffset = new Vector3(
	    //    (0.5f - this.GetComponent<RectTransform>().pivot.x) * this.GetComponent<RectTransform>().rect.size.x,
	    //    (0.5f - this.GetComponent<RectTransform>().pivot.y) * this.GetComponent<RectTransform>().rect.size.y,
	    //    0f);

	    //Debug.Log("###" + pivotOffset);

     //   Debug.Log("###############" + this.GetComponent<RectTransform>().sizeDelta);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
